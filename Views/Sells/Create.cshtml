@model Mobiles_MVC.Models.Sell

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Sell</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ProductsId" class="control-label"></label>
                <select asp-for="ProductsId" class ="form-control" asp-items="ViewBag.ProductsId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CustomersId" class="control-label"></label>
                <select asp-for="CustomersId" class ="form-control" asp-items="ViewBag.CustomersId"></select>
            </div>
            <div class="form-group">
                <label asp-for="StaffsId" class="control-label"></label>
                <select asp-for="StaffsId" class ="form-control" asp-items="ViewBag.StaffsId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
